div(class="capture")
  div(class="container")
    div(class="capture__content")
      h2(class="capture__title") Capture the moment
      div(class="separator separator--left")
        div(class="separator__container")
          svg(class="separator__square" width="18" height="12")
            rect(width="18" height="12")
          div(class="separator__bar")
      p(class="capture__description") The new GoPro HERO 6 camera can capture super-slow-motion video at a high resolution, output a stabilized image in 4K, and transfer everything to your phone at faster speeds – your video can even be posted before your GoPro-documented adventure is even over if you wish. This is the only action camera that's this tiny and durable.
  div(class="capture__visual")
    div(class="capture__shape")
    div(class="capture__wrapper")
      div(class="capture__window")
      div(class="capture__window2")
      video(class="capture__source lazyload" autoplay loop playsinline data-src="assets/video/capture-the-moment.mp4")